node_modules/
out/
dist/
resources/native/win-x64/*.exe
resources/native/win-x64/*.sha256
resources/icon.icns
*.local
.eslintcache

# Agent skill 安装产物（由 skills-lock.json 重建，如同 node_modules 由 lockfile 重建）
.agents/

# Local third-party visual research mirror (never package or commit)
cineshader-mirror/

# Local Mineradio reference tree (never package or commit)
Mineradio/

# Local harness scratch / build artifacts (never commit)
.dsh-tmp/
