/** Camera radius bounds: a smaller radius makes the 3D lyrics appear larger. */
export const LYRICS_ZOOM_MIN_RADIUS = 6.4
export const LYRICS_ZOOM_MAX_RADIUS = 18

/** Synthetic outline expansion. Keep within 0–0.03 to avoid closing dense glyph counters. */
export const LYRICS_FONT_WEIGHT = 0.008
